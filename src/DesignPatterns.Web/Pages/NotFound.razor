@page "/not-found"
@layout MainLayout

<PageTitle>404 - No Encontrado</PageTitle>

<div class="flex flex-col items-center justify-center min-h-[60vh] px-4 py-8">
    <!-- Error Code -->
    <div class="text-6xl md:text-8xl lg:text-9xl font-bold bg-gradient-to-r from-primary-600 to-accent-purple bg-clip-text text-transparent mb-4 md:mb-6">
        404
    </div>

    <!-- Error Message -->
    <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold text-secondary-900 mb-2 md:mb-3 text-center">
        Página No Encontrada
    </h1>
    
    <p class="text-base md:text-lg lg:text-xl text-secondary-600 mb-6 md:mb-8 text-center max-w-md">
        Lo sentimos, la página que buscas no existe o ha sido movida.
    </p>

    <!-- Suggestion -->
    <div class="bg-blue-50 border-l-4 border-primary-600 p-4 mb-6 md:mb-8 rounded-lg w-full max-w-md">
        <p class="text-secondary-700 text-xs md:text-sm">
            <HeroIcon IconName="light-bulb" AdditionalClass="w-4 h-4 inline mr-1" />
            <strong>Sugerencia:</strong> Intenta navegar usando el menú lateral o vuelve a la página de inicio.
        </p>
    </div>

    <!-- Home Button -->
    <a href="/" class="w-full max-w-md px-4 md:px-6 py-3 md:py-4 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-colors shadow-md hover:shadow-lg text-center text-sm md:text-base mb-8 md:mb-10">
        ← Volver al Inicio
    </a>

    <!-- Explore Patterns Section -->
    <div class="w-full max-w-2xl">
        <h2 class="text-2xl md:text-3xl font-bold text-secondary-900 mb-2 text-center">
            Explorar Patrones
        </h2>
        
        <p class="text-secondary-600 text-base md:text-lg mb-6 md:mb-8 text-center">
            Descubre las tres categorías principales de patrones de diseño
        </p>

        <div class="flex flex-col sm:flex-row gap-3 md:gap-4 w-full justify-center">
            <!-- Creational Button -->
            <a href="/creational" class="flex-1 px-4 md:px-6 py-2 md:py-3 bg-primary-600 text-white font-semibold rounded-lg hover:bg-primary-700 transition-all shadow-sm hover:shadow-md text-center text-xs md:text-sm">
                Creational ←
            </a>

            <!-- Structural Button -->
            <a href="/structural" class="flex-1 px-4 md:px-6 py-2 md:py-3 bg-accent-green text-white font-semibold rounded-lg hover:bg-emerald-700 transition-all shadow-sm hover:shadow-md text-center text-xs md:text-sm">
                Structural ←
            </a>

            <!-- Behavioral Button -->
            <a href="/behavioral" class="flex-1 px-4 md:px-6 py-2 md:py-3 bg-accent-purple text-white font-semibold rounded-lg hover:bg-purple-700 transition-all shadow-sm hover:shadow-md text-center text-xs md:text-sm">
                Behavioral ←
            </a>
        </div>
    </div>

    <!-- Illustration -->
    <div class="mt-12 md:mt-16 flex justify-center opacity-50">
        <HeroIcon IconName="magnifying-glass" AdditionalClass="w-24 h-24 md:w-32 md:h-32 lg:w-40 lg:h-40 text-secondary-400" />
    </div>
</div>